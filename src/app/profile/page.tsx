import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Profile",
};

const ProfilePage = () => {
  return <div className={"main-p w-full h-full"}>Profile page</div>;
};

export default ProfilePage;
