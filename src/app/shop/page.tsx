import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Shop",
};

const ShopPage = () => {
  return <div className={"main-p w-full h-full"}>Shop page</div>;
};

export default ShopPage;
