import { MainContainer } from "./components";
import { BlockWrapper, Subtitle } from "@/shared";

export const Difference = () => {
  return (
    <BlockWrapper>
      <Subtitle text={"Spot the Difference?"} />

      <MainContainer />
    </BlockWrapper>
  );
};
